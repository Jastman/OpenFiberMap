# OpenFiberMap ETL dependencies
# Install with: pip install -r requirements.txt

# HTTP client with retry + connection pooling
requests>=2.31.0
urllib3>=2.0.0

# JSON Schema validation
jsonschema>=4.21.0

# Geospatial (optional — used if converting SHP or doing spatial operations)
# Uncomment if running the OSM / ANATEL SHP converters:
# geopandas>=0.14.0
# shapely>=2.0.0
# pyproj>=3.6.0
# fiona>=1.9.0

# Utilities
python-dateutil>=2.8.0
